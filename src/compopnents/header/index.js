import React from 'react';
import TopBar from './top-bar';
import './styles.scss'

const Header = () => {
  return (
    <section className="header">
      <TopBar/>
    </section>
  );
};

export default Header;